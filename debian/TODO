TODO list for dietlibc in Debian
--------------------------------

Package features:

 - reenable dynamic linking support on architectures
   that support it
 - enable PIE support in dietlibc once that's supported upstream
